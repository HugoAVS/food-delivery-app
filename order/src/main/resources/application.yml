server:
  port: 9094

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: ORDER-SERVICE

  data:
    mongodb:
      uri:  mongodb+srv://orderuser:123@cluster0.nodgvej.mongodb.net/orderdb?retryWrites=true&w=majority
      host: localhost
      port: 27017
      database: orderDB
      repositories:
        enabled: true